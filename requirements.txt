# DC-ASR core dependencies (Python 3.10+, CUDA GPU).
# Pins intentionally loose here; freeze exact versions once the env is validated on Babel.

# --- Sequence-model backbone ---
torch>=2.1
torchaudio>=2.1
mamba-ssm>=2.2        # Mamba-2 selective SSM (Gu & Dao); needs matching CUDA
causal-conv1d>=1.2    # fast causal conv kernel required by mamba-ssm

# --- ASR / data ---
sentencepiece>=0.1.99 # BPE units
editdistance>=0.6     # WER scoring
soundfile>=0.12
librosa>=0.10         # feature extraction / resampling
numpy>=1.24
scipy>=1.10

# --- Config, logging, training ---
pyyaml>=6.0
omegaconf>=2.3
tensorboard>=2.14
tqdm>=4.66

# --- Interpretability / analysis ---
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3     # linear probes

# NOTE — installed separately, NOT via pip:
#   * Montreal Forced Aligner (MFA)  -> conda: `conda install -c conda-forge montreal-forced-aligner`
#   * An external LM (Transformer/n-gram) for the +LM decode columns.
